{
    "scripts": {
        "update-library": "cd library && npm run build && npm pack && cd ../crm && npm i",
        "_update-library": "cd library && npm run build && npm pack && cd ../generator && npm i"
    }
}